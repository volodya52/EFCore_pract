<Page x:Class="EFCorePract.Pages.UsersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:EFCorePract.Pages"
      xmlns:validators="clr-namespace:EFCorePract.Validators"
      xmlns:service="clr-namespace:EFCorePract.Service"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UsersPage"
      >

    <Page.Resources>
        <service:RoleService x:Key="rService"/>
    </Page.Resources>

    <Grid>
        <StackPanel>
            <StackPanel>
                <Label Style="{DynamicResource Labels}" >Логин</Label>
                <TextBox Style="{DynamicResource Login}">
                    <TextBox.Text>
                        <Binding Path="Login" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:EmptyText/>
                                <validators:LoginCheck/>
                                <validators:CheckLoginTwice/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </StackPanel>
            <StackPanel>
                <Label Style="{DynamicResource Labels}">Имя</Label>
                <TextBox Style="{DynamicResource Name}">
                    <TextBox.Text>
                        <Binding Path="Name" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:EmptyText/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </StackPanel>
            <StackPanel>
                <Label Style="{DynamicResource Labels}">Email</Label>
                <TextBox Style="{DynamicResource Email}">
                    <TextBox.Text>
                        <Binding Path="Email" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:EmptyText/>
                                <validators:CheckEmail/>
                                <validators:CheckEmailTwice/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </StackPanel>
            <StackPanel>
                <Label Style="{DynamicResource Labels}">Пароль</Label>
                <TextBox Style="{DynamicResource Password}">
                    <TextBox.Text>
                        <Binding Path="Password" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:EmptyText/>
                                <validators:CheckPassword/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </StackPanel>
            <StackPanel>
                <Label Style="{DynamicResource Labels}">Дата создания</Label>
                <DatePicker Style="{DynamicResource DateTimePicker}">
                    <DatePicker.SelectedDate>
                        <Binding Path="CreatedAt" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:CheckDate/>
                            </Binding.ValidationRules>
                        </Binding>
                    </DatePicker.SelectedDate>
                </DatePicker>
            </StackPanel>
            <StackPanel>
                <Label Style="{DynamicResource Labels}">Ссылка на изображение</Label>
                <TextBox Style="{DynamicResource Login}">
                    <TextBox.Text>
                        <Binding Path="UserProfile.AvatarUrl" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:EmptyText/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </StackPanel>
            <StackPanel>
                <Label Style="{DynamicResource Labels}">Номер телефона</Label>
                <TextBox Style="{DynamicResource Login}">
                    <TextBox.Text>
                        <Binding Path="UserProfile.Phone" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:EmptyText/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </StackPanel>
            <StackPanel>
                <Label Style="{DynamicResource Labels}">Дата рождения</Label>
                <DatePicker Style="{DynamicResource DateTimePicker}">
                    <DatePicker.SelectedDate>
                        <Binding Path="UserProfile.Birthday" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:CheckDate/>
                            </Binding.ValidationRules>
                        </Binding>
                    </DatePicker.SelectedDate>
                </DatePicker>
            </StackPanel>
            <StackPanel>
                <Label Style="{DynamicResource Labels}">Биография</Label>
                <TextBox Style="{DynamicResource Login}">
                    <TextBox.Text>
                        <Binding Path="UserProfile.Bio" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:EmptyText/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </StackPanel>
            <StackPanel>
                <Label Style="{DynamicResource Labels}">Роль:</Label>
                <ComboBox Style="{DynamicResource RoleComboBox}"
                          ItemsSource="{Binding Source={StaticResource rService}, Path=Roles}"
                          DisplayMemberPath="Title"
                          SelectedItem="{Binding Role}"/>
            </StackPanel>
            <StackPanel>
                <Button Style="{DynamicResource SaveButton}" Click="SaveUser">Сохранить</Button>
                <Button Style="{DynamicResource BackButton}" Click="GoBack">Назад</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
