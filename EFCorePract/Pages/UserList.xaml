<Page x:Class="EFCorePract.Pages.UserList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EFCorePract.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{Binding RelativeSource={RelativeSource Self}}"
      Title="UserList">



    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2">
            <Button Style="{DynamicResource AddButton}" Click="GoEditPage">Добавить</Button>
            <Button Style="{DynamicResource DeleteButton}" Click="Delete">Удалить</Button>
            <Button Style="{DynamicResource AddButton}" Click="GoRole">Роли</Button>
            <Button Style="{DynamicResource GroupsButton}" Click="GoGroupsList">Группы интересов</Button>
            <Button Style="{DynamicResource AddButton}" Click="AddinGroup">Добавить в группу</Button>
        </StackPanel>

        <ListView Grid.Row="1"
                  ItemsSource="{Binding service.Users}"
                  MouseDoubleClick="EditUser"
                  SelectedItem="{Binding user}"
                  Style="{DynamicResource ListViewStyle}">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Логин" DisplayMemberBinding="{Binding Login}"/>
                        <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}"/>
                        <GridViewColumn Header="Пароль" DisplayMemberBinding="{Binding Password}"/>
                        <GridViewColumn Header="Дата создания" DisplayMemberBinding="{Binding CreatedAt, StringFormat={}{0:dd.MM.yyyy}}"/>
                        <GridViewColumn Header="Аватар">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Source="{Binding UserProfile.AvatarUrl, TargetNullValue={x:Null}}" Width="100" Height="100"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Адрес аватара" DisplayMemberBinding="{Binding UserProfile.AvatarUrl}"/>
                        <GridViewColumn Header="Номер телефона" DisplayMemberBinding="{Binding UserProfile.Phone, StringFormat={}{0: # ### ### ## ##}}"/>
                        <GridViewColumn Header="Дата рождения" DisplayMemberBinding="{Binding UserProfile.Birthday, StringFormat={}{0:dd.MM.yyyy}}"/>
                        <GridViewColumn Header="Биография" DisplayMemberBinding="{Binding UserProfile.Bio}"/>
                        <GridViewColumn Header="Роль" DisplayMemberBinding="{Binding Role.Title}"/>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>

        <ListView
                  ItemsSource="{Binding service2}"
                  Grid.Row="1"
                  Grid.Column="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <Run Text="Группа"/>
                        <Run Text="{Binding Title}"/>
                        <Run Text="Описание"/>
                        <Run Text="{Binding Description}"/>
                        <Run Text="Модератор"/>
                        <Run Text="{Binding IsModerator}"/>
                    </TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
